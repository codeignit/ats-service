spring:
  application:
    name: ats

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 2MB

  docker:
    compose:
      enabled: false

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: mistral:7b